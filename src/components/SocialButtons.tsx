"use client";

import type { SocialLink } from "@/lib/types";

/** I social ufficiali stanno nella barra in alto. In home non si ripetono. */
export default function SocialButtons({
  links: _links,
  className: _className,
}: {
  links?: SocialLink[];
  className?: string;
}) {
  return null;
}
